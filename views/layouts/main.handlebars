<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The TBF</title>
  <link rel="stylesheet" href="\css\jass.css">
</head>

<body>
  <header class="display-flex justify-space-between align-left">
    <nav>
     <a href="/">Home</a>
    {{#if logged_in}}
    <a href="/dashboard">Dashboard</a>
    {{else}}
    <a href="/login">Dashboard</a>
    {{/if}}
    {{#if logged_in}}
    <a id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>
    <h1 id="main-header">
     Tech Blog Frog
    </h1>
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>