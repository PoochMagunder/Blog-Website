{{#each posts as |post|}}
<div>
  <h2>{{post.title}} Created On:{{post.date.created}}</h2>
  <p>{{post.content}}</p>
</div>
{{/each}}
<div>
  <button id="new-post-home" class="btn-no-style" href="/dashboard">New Post</button>
</div>